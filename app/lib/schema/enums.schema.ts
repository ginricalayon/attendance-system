export enum DBCollections {
  STUDENTS = "students",
}

export enum Departments {
  CCS = "CCS",
  COE = "COE",
  CBAA = "CBAA",
  COHM = "COHM",
  SHS = "SHS",
}

export enum OrderBy {
  ASC = "asc",
  DESC = "desc",
}
