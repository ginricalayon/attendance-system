export enum DBCollections {
  STUDENTS = "students",
  EVENTS = "events",
}

export enum Departments {
  CCS = "CCS",
  COE = "COE",
  CBAA = "CBAA",
  COHM = "COHM",
  SHS = "SHS",
}

export enum OrderBy {
  ASC = "asc",
  DESC = "desc",
}

export enum EventTypes {
  LOGIN = "login",
  LOGOUT = "logout",
}
